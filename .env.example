# URL del backend. OJO: Vite la resuelve en tiempo de BUILD, no de ejecucion.
# Cambiar de ambiente exige recompilar el frontend.
#
# En desarrollo se deja vacia para usar el proxy de Vite (mismo origen).
# En produccion: VITE_API_URL=https://api.midominio.com
VITE_API_URL=
